#pragma once

// espidf includes
#include "esp_event.h"

ESP_EVENT_DECLARE_BASE(SYSTEM_EVENT_BASE);

/*
 *	Events
 */
enum SYSTEM_EVENT_ID
{
	/*
	 *	System
	 */
	REGISTRATION_COMPLETED,
	DISPLAY_REGISTERED,

	/*
	 *	Update
	 */
	DISPLAY_UPDATE_DOWNLOADED,

	/*
	 *	CAN
	 */
	CAN_FRAME_RECEIVED,

	/*
	 *	KLine
	 */
	KLINE_MSG_RECEIVED,

	/*
	 *	Sensors
	 */
	ACTIVE_SENSOR_VALUE_CHANGED,
	PASSIVE_SENSOR_VALUE_CHANGED,
	FUEL_LEVEL_CHANGED,
	LEFT_INDICATOR_ACTIVE_CHANGED,
	RIGHT_INDICATOR_ACTIVE_CHANGED,
	OIL_PRESSURE_CHANGED,
	RPM_CHANGED,
	SPEED_CHANGED,
	WATER_TEMP_CHANGED,
};
